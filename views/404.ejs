<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang==='ar' ? 'الصفحة غير موجودة - 404' : 'Page Not Found - 404' %> - Mentorship STEM</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header', { lang: lang }) %>

    <div class="error-container" style="min-height: 60vh; display: flex; flex-direction: column; justify-content: center; align-items: center; padding: 2rem;">
        <div class="error-content" style="text-align: center; max-width: 600px;">
            <h1 style="font-size: 3rem; margin-bottom: 1rem; color: var(--primary-color);">404</h1>
            <h2 style="font-size: 2rem; margin-bottom: 1rem;">
                <%= lang==='ar' ? 'الصفحة غير موجودة' : 'Page Not Found' %>
            </h2>
            <p style="font-size: 1.2rem; margin-bottom: 2rem; color: var(--text-secondary);">
                <%= lang==='ar' ? 'عذراً، لم نتمكن من العثور على الصفحة التي تبحث عنها.' : 'Sorry, we could not find the page you are looking for.' %>
            </p>
            <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
                <a href="/" class="btn btn-primary" style="display: inline-block; padding: 0.8rem 1.5rem; text-decoration: none; border-radius: var(--radius-md);">
                    <%= lang==='ar' ? 'العودة للرئيسية' : 'Go Home' %>
                </a>
                <a href="/contact" class="btn btn-primary" style="display: inline-block; padding: 0.8rem 1.5rem; text-decoration: none; border-radius: var(--radius-md); background: var(--secondary-color); color: black;">
                    <%= lang==='ar' ? 'تواصل معنا' : 'Contact us' %>
                </a>
            </div>
        </div>
    </div>

    <%- include('partials/footer', { lang: lang }) %>
</body>
</html>
